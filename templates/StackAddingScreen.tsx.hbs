
      <Stack.Screen
        name="{{extractName name}}"
        component={{preCurly (extractName name)}}
        initialParams=\{{
          title: "{{extractName name}}",
        }}
      />
    </Stack.Navigator>