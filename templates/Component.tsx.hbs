import { View } from "react-native";
import Text from "@/components/general/Text";
import { {{extractName name}}Props } from "./types";
import styles from "./styles";

export default function {{extractName name}}({ containerStyle }: {{extractName name}}Props) {
  return (
    <View style={[styles.container, containerStyle]}>
      <Text>{{extractName name}}</Text>
    </View>
  );
}
